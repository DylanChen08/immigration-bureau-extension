# 安装和构建说明

## 📦 安装 pnpm

如果还没有安装 pnpm，可以使用以下任一方法：

### 方法 1：使用 npm 全局安装（推荐）

```bash
npm install -g pnpm
```

### 方法 2：使用核心包管理器（Corepack）

Node.js 16.13+ 自带 corepack，可以启用它：

```bash
corepack enable
```

然后直接使用 `pnpm` 命令即可。

### 方法 3：使用独立安装脚本

```bash
curl -fsSL https://get.pnpm.io/install.sh | sh -
```

Windows (PowerShell):
```powershell
iwr https://get.pnpm.io/install.ps1 -useb | iex
```

## 🚀 安装项目依赖

安装 pnpm 后，运行：

```bash
pnpm install
```

## 🔨 构建项目

```bash
pnpm build
```

构建完成后，`dist/` 目录就是可以加载到 Chrome 的扩展文件。

## 📝 验证安装

运行以下命令确认 pnpm 已正确安装：

```bash
pnpm --version
```

应该显示版本号（如：8.0.0）

---
